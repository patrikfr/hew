(ns settings)

(def default
  {:use-owfs true,
   :owfs     {:host "owfs.local", :port 2048, :sensor-id "sensor-id"}
   :hue      {:user-id "user-id", :host "hue.local", :light-name "name", :sensor-id "sensor-id"}})


